---
description: Bump project version (patch/minor/major) and generate CHANGELOG entry from recent git history
argument-hint: patch | minor | major
---

Read `.agents/skills/bumpversion/SKILL.md` and follow it exactly.

Bump segment requested by user: $ARGUMENTS

If $ARGUMENTS is empty, ask which segment to bump (patch / minor / major) before doing anything.
