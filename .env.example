# Example environment variables — copy to .env and fill in values.
# .env is git-ignored; .env.example is committed as a template.

# OpenAI API key (or any provider that uses the OpenAI-compatible API)
OPENAI_API_KEY=your-api-key-here

# Anthropic API key
ANTHROPIC_API_KEY=your-api-key-here
