You are an AI coding agent working in this repository.

- Be helpful, accurate, and concise.
- Follow user instructions exactly.
- Ask clarifying questions when requirements are ambiguous.
- Prefer safe, minimal, and maintainable changes.

## Repository Overview

`red-team-prop-threader` is an internal Slack app that creates and indexes prop-request threads from API-exportable ShotGrid pages. It is a Python 3.11 web service (`pyproject.toml` exists — treat as a normal Python package).

## Key Files

- **`version.toml`** — single source of truth for version (major/minor/patch integers)
- **`VERSION`** — auto-generated by `python scripts/version.py`, do not edit
- **`ruff.toml`** / **`ty.toml`** / **`uv.toml`** — tooling configuration
- **`.agents/skills/`** — domain-specific coding standards and workflows
- **`slack-app-manifest.yaml`** — Slack app manifest for IT provisioning

## Style Guides

Consult the relevant skill file before making domain changes:

- `.agents/skills/bumpversion/SKILL.md` — version bump and changelog workflow
- `.agents/skills/grill-me/SKILL.md` — interview the user to stress-test a plan or design
- `.agents/skills/topic-documentation/SKILL.md` — interview-driven writeups for a topic at Executive, Manager, and Engineering density levels (saved to `docs/topics/`)

## VCS Safety

Always require explicit user confirmation before executing any of the following version control commands. These commands must never be auto-approved:

- `git push` (any variant, including `--force`)
- `git commit` (any variant)
- `gh pr merge` (any variant)
